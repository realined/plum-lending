# Build journal

This journal records architectural checkpoints and verified results. Code being present is not proof of completion. Live acceptance is mandatory and remains pending until explicitly authorized real-account testing passes.

## Phase 1 — Domain foundation

**Goal.** Convert a natural-language request into reviewed constraints and determine contact/thread eligibility without AI decisions.

**Decisions.** Strict TypeScript; Zod for intent/spec validation; frozen as-of UTC instant; calendar-month arithmetic; exact case-insensitive identities; direct contact-to-deal association scope; current Closed Won stage excludes across all time. Exact reference header is `Email Body`, populated with complete thread text.

**Implementation.** `src/domain/segment.ts`, `models.ts`, `identity.ts`, `executor.ts`, and `csv.ts`; synthetic positive/negative dataset in `src/providers/demo.ts`; provider-independent contracts in `src/providers/contracts.ts`.

**Data flow.** Reviewed SegmentSpec + CRM snapshot + full threads → deterministic sender/recipient/date/deal checks → one row per contact/thread → quoted, formula-safe CSV with normalized JSON.

**Validation.** 60 domain tests passed. Tests exposed and prompted fixes for invalid date refinement throwing, fractional-second timestamp comparison/order, and repeated message IDs in export evidence. No live credentials or correspondence used.

**Alternatives.** SQL generated by an LLM or autonomous provider tools were rejected because validation, explainability, and authorization are weaker. Concatenating an entire contact history into one cell was rejected in favor of contact/thread rows. Fuzzy identity matching was rejected because false joins could expose or misattribute correspondence.

**Assumptions and remaining work.** Current stage represents Closed Won; historical reopened wins are outside this slice. Live role property and aliases must be confirmed during setup. MIME/provider contract tests and end-to-end validation remain separate gates.

**Interview talking points.** Runtime versus TypeScript validation; AI interpretation versus deterministic execution; inclusive/exclusive time boundaries; identity precision; thread-level evidence and exclusion reasons.

## Phase 2 — Persistence, integrations, and jobs (verification in progress)

**Goal.** Retrieve provider data safely and make long-running exports durable.

**Decisions.** PostgreSQL schema with Drizzle; PGlite for a one-process credential-free demo; ordinary PostgreSQL plus a dedicated worker for live mode. Atomic claims, expiring leases, heartbeats, and owner fencing. AES-256-GCM tokens. Google authorization code + PKCE + expiring state; Gmail read-only scope. HubSpot private-app token validated server-side.

**Implementation.** `src/db/schema.ts`, migration SQL, `src/server/database.ts`, `repository.ts`, `worker.ts`, `connections.ts`, `oauth.ts`, `auth.ts`, and `src/providers/{gmail,hubspot,http}.ts`. Provider responses are translated into canonical entities. Association uncertainty fails closed.

**Alternatives.** Redis/BullMQ would add a second service for a workload adequately handled by a PostgreSQL queue. Request-local background promises were rejected because they do not survive process termination. Provider SDKs were avoided for a small read-only surface with explicit Zod response validation.

**Validation.** Domain tests pass; provider, MIME, storage/recovery, API, and browser tests are pending. Type checking found a driver generic cast mismatch, corrected at the database boundary; full checks will be rerun.

**Assumptions and remaining work.** Single workspace and one Gmail identity; configured lender aliases. Gmail history adapter exists but continuous materialized sync is not yet scheduled. Real Gmail, HubSpot, and OpenAI have not been connected. Live testing is mandatory, with explicit user authorization before connection or processing.

**Security.** Tokens server-side, encrypted at rest; sessions signed; mutation origins checked; no message or token logging. Local data deletion fences jobs by removing parent records. Deployment hardening and retention remain documented production work.

**Interview talking points.** At-least-once processing; idempotency versus exactly-once claims; lease fencing; fail-closed CRM eligibility; provider normalization boundaries.

## Phase 3 — Admin workflow (implementation in progress)

**Goal.** Make interpretation and evidence reviewable before and after execution.

**Decisions.** Server stores the interpretation; the browser runs its ID rather than submitting an arbitrary spec. Poll durable state; explicit partial/empty/error states; paged preview and streamed CSV. Synthetic scenarios visibly labeled.

**Implementation.** App Router API and `src/components/workspace.tsx`, `results.tsx`, responsive CSS. Connection, interpretation, job, history, preview, full-thread drawer, and download views.

**Validation.** Pending type/lint/build and Playwright flows and visual inspection.

**Assumptions.** Single administrator; shared-password local POC authentication. No public deployment. No live data processed.

**Interview talking points.** Human review at the interpretation boundary; immutable request-time semantics; partial results versus completeness; evidence-first UI; sensitive-content rendering as text.


## Offline phase checkpoint — verified 2026-09-16

**Phase 2 implemented and offline-verified.** 136 total Vitest tests pass, including provider, MIME, real SQL storage, crypto/auth and account-boundary tests. Database tests verify enqueue deduplication, canonical upserts, partial/empty/outage results, rollback, expired leases, owner fencing, retry-exhaustion cleanup and purge fencing. Reconnect generation and concurrent-refresh tests validate account isolation. Native PostgreSQL and live provider acceptance are still required.

**Defects found and fixed.** Primary-email alias ordering, custom Sponsor mapping collision, unnecessary text-attachment download, demo/live database crossover, reconnecting across accounts during a run, peer-refresh compare-and-swap rejection, incomplete HubSpot permission checks, non-PostgreSQL live URLs, and orphaned running sync-attempt status after retry exhaustion. On-disk app startup exposed a missing parent-directory creation that in-memory database tests could not reveal; startup now creates private directories recursively.

**Phase 3 implemented and interactively verified.** The admin UI is split into focused modules below 350 lines. Stored run criteria remain inspectable in history. Readability was adjusted after visual review. The supported in-app browser verified interpretation → run → exact counts → full-thread inspection → download action; also partial, empty, outage and unsupported states, and saved history after a process restart. Mobile view has no page-level horizontal overflow and preserves navigation. Synthetic screenshots are the only screenshots saved.

**Automated application evidence.** Three Playwright API tests pass against `next start`, including the stored-job pipeline, CSV response, idempotent submission, partial/empty/outage semantics, origin rejection, schema rejection, nonexistent exports and oversized input. Lint, strict types, production build and production dependency audit pass; audit reports no known vulnerabilities.

**Browser-runner limitation.** Four repository Playwright UI tests are implemented, but standalone Chromium cannot launch in this macOS sandbox (Mach bootstrap permission denied). Earlier `next dev` also exceeded the environment's watcher limit; testing moved to the production server. Supported in-app browser checks passed, but that does not turn the blocked runner suite into a pass. Run the suite on a normal local machine before submission.

**Phase 4 handoff.** Setup/readme, architecture, assumptions, security, production adapters/scaling, synthetic CSV/expected data, demo script, and 60-second/5-minute/20-minute interview walkthrough are written. A manifest-based packager excludes runtime databases, `.env`, dependencies, build output, traces and logs. The archive is a development handoff; it is not evidence that live acceptance is complete.

**Remaining mandatory work.** Owner-controlled PostgreSQL and account setup; explicit permission to connect/process; Gmail OAuth; HubSpot token validation; live OpenAI interpretation; real ingestion; independent eligibility/full-thread/CSV checks; final sanitized acceptance record. No real service has been connected. Do not mark project complete until these pass.

**Interview talking points.** Layered evidence and honest completion gates; schema validity versus semantic correctness; adapter/account isolation; durable work and fencing; exporting context without sending it to AI. Explain why a memory-only test missed a disk startup bug and why real account configuration cannot be validated by mocks.

**Owner action next.** Follow `live-setup.md`, keep all secrets local, then authorize the defined live acceptance procedure. The agent pauses before account connection/processing; this pause is required by the owner's collaboration protocol, not inferred from a skill.

## Version-control checkpoint — 2026-09-16

**Goal.** Preserve the verified application in reproducible Git history and prepare an owner-approved remote backup.

**Finding and implementation.** Git had been initialized and 83 source files staged, but no commit or remote existed. This was an incomplete handoff. The reviewed source is now committed on `main` as `2be9c79` (`feat: establish Plum Lending proof-of-concept baseline`). README now explains the working-copy workflow and the difference between source ZIPs and Git history; the execution checklist tracks remote setup separately.

**Decisions and data flow.** Keep the existing project location and `main` branch. Reviewed source moves from working tree to staging to local commit; a remote push remains pending. Recommend a private GitHub repository to keep interview work under the owner's control. Reinitializing or moving the project adds no value; a ZIP alone cannot preserve history. No runtime or provider data belongs in this flow.

**Validation.** All 83 staged paths matched the reviewed archive manifest. A scan found no recognized credential patterns. `git diff --cached --check` passed; the initial commit left a clean checkout. Ignore checks verified `.env`, `.env.local`, `.data`, and live exports are excluded, with `.env.example` tracked. `git fsck --no-reflogs` found no corrupt objects; two harmless dangling blobs came from earlier staging. No application code changed and the previously recorded application test results remain the latest evidence.

**Assumptions, limitations, and remaining work.** Use the existing configured Git author identity. There is no remote backup until a push is verified. The authenticated GitHub connector identifies the account as `realined`; querying `realined/plum-lending` returned not found or inaccessible, so no remote is assumed to exist. Confirm destination/visibility before uploading source. No live accounts have been connected; all mandatory live acceptance gates remain open.

**Security and interview talking points.** Explain working tree versus staging versus commits; local history versus remote backup; feature branches and reviewable changes; why ignore rules do not remove already tracked secrets. Pattern scanning is a useful check, not a guarantee that arbitrary sensitive text cannot enter a commit.

**Owner action.** Confirm a private GitHub destination or supply the intended existing repository. This approval concerns remote publication only and does not authorize live Gmail or HubSpot processing.

## Project relocation and remote preparation — 2026-09-16

**Goal.** Put the complete project in the owner's normal Projects directory and publish the existing history to the explicitly approved private `realined/plum-lending` repository.

**Decisions and implementation.** Moved the entire checkout to `~/Projects/plum-lending`, including `.git`, ignored local demo data, and dependencies. Stopped the demo before the move and restarted it afterward. Kept the existing history rather than creating a second repository. Configured the approved HTTPS URL as `origin`; remote creation/upload remains pending authentication.

**Validation.** The destination did not exist, so nothing was overwritten. The move preserved commit `39bcb2b` and a clean working tree. pnpm's dependency cache changed with the parent directory; reinstalled the exact frozen lockfile into the new location. After relocation, lint, strict type checking, all 136 Vitest tests, and the production build passed again. Standard `pnpm start` works, and the demo returned HTTP 200 at localhost:3000. No application source or dependency version changed.

**Data flow and alternatives.** Git history and the working directory move together; dependencies and build output can be regenerated from checked-in source and the lockfile. A symlink or copied second checkout would leave confusing competing locations, so an actual move was selected. The demo remains synthetic and account setup/live acceptance remains pending.

**Remote blocker and security.** The connected GitHub tool cannot create repositories, the browser is signed out, and Git has no HTTPS credential available. Installed the official GitHub CLI in the task's work folder and verified its release SHA-256. The owner must finish the CLI's browser login; no password or token is requested in chat. A configured remote is not a remote backup. Existing ignore rules still apply, and source-only packaging remains separate from private runtime data.

**Remaining work and owner action.** Complete GitHub CLI sign-in as `realined`. Then create the private repository, push existing history, verify its visibility and matching commit SHA, and update this checkpoint. The owner has already authorized repository creation and pushing; no second publication approval is needed.

**Interview talking points.** A Git repository is portable because history lives in `.git`; lockfiles preserve dependency versions while caches are disposable; remote configuration versus a verified push; authentication versus authorization; synthetic verification versus mandatory live acceptance.

## Private GitHub publication — verified 2026-09-16

**Goal.** Complete the approved remote backup while preserving the existing local commit history.

**Implementation and decisions.** After the owner completed GitHub CLI sign-in, verified the authenticated account was `realined`, created the private repository at https://github.com/realined/plum-lending, and pushed local `main` using the existing HTTPS `origin`. Local `main` now tracks `origin/main`. README and the checklist reflect the verified remote. Kept the local checkout at `~/Projects/plum-lending`.

**Data flow and alternatives.** Reviewed source commits travel through authenticated Git to the private remote; ignored runtime files do not participate. Used an ordinary push to retain all existing commits. Rebuilding files through an API or uploading a ZIP would not preserve the same history and was unnecessary once CLI authentication was available.

**Validation.** GitHub reports `isPrivate: true` and default branch `main`. The first push completed successfully, and `git ls-remote` matched local HEAD `2b41c551725e5c4877ab98cba2fb8dabf9e99e64`. The working tree was clean and the branch had its upstream set. All 83 tracked files match the reviewed manifest; private/runtime path checks and recognized credential-pattern scanning passed. No application code changed during publication; the relocation checkpoint's passing lint, type checks, 136 tests, and production build remain the latest application verification.

**Security, assumptions, and limitations.** GitHub credentials were used through the authenticated CLI/Git credential mechanism without printing or committing them. Repository access remains private. Publishing source does not deploy the application, configure Gmail/HubSpot, or prove live integration. GitHub availability and account access are still external dependencies, and pattern scanning cannot detect every kind of sensitive content.

**Remaining work and owner action.** No additional repository approval or action is required. Mandatory Gmail OAuth, HubSpot connection/ingestion, live CSV acceptance, and the previously blocked standalone browser suite remain on the execution plan. Live data processing still requires the owner's explicit authorization.

**Interview talking points.** Local commits versus a remote backup; upstream tracking and branch SHA verification; preserving history with ordinary pushes; private repository access versus application credentials; source publication versus deployment and live acceptance.

## Requirements and interaction audit — 2026-09-16

**Goal.** Reconcile the challenge with observable behavior after the owner reported inactive navigation/menu elements; produce an evidence-based next phase instead of treating demo success as completion.

**Findings and implementation.** Added `docs/requirements-audit.md` with a requirement matrix, control inventory, data flow, gaps, and completion gates. Updated the execution checklist. The three real sidebar buttons and thread drawer work. Workspace chevron, breadcrumb, top-right avatar and lower-left profile are static shell elements; only the live-mode sign-out button has profile behavior. Demo connection buttons are hidden and the parser is rules-based. No application behavior changed in this audit.

**Decisions and alternatives.** Preserve the current stack and single-workspace scope. Fix misleading controls and setup guidance before live account acceptance; do not fill empty menus with unrelated settings or implement multitenancy. Treat code, mocked contracts, synthetic end-to-end execution, and real-account proof as separate evidence. Do not declare all integrations complete based on tests.

**Validation.** Compared the original request and owner amendments with saved challenge text (fresh challenge retrieval returned HTTP 403), inspected implementation and test boundaries, and verified all sidebar tabs plus full chronological thread context in the supported browser. Fresh isolated production API tests: 3 passed in 6.1 seconds. Prior unchanged-source evidence: 136 Vitest tests, lint, types, production build passing. Standalone browser launch remains unverified. No live data processed and no secrets inspected.

**Assumptions, risks and remaining work.** Core workflow is an on-demand snapshot export, not continuous synchronization. Gmail history/cursor support is not invoked or advanced by the worker. OAuth start/callback and OpenAI response-boundary tests are absent. Gmail currently retrieves every date-window candidate thread before contact matching, including when no CRM contacts qualify; review retrieval breadth and measure runtime before personal-mail ingestion. Improve sanitized errors/readiness and separate new drafts from old results. Native PostgreSQL/separate worker and mandatory live Gmail/HubSpot/OpenAI/export acceptance remain pending.

**Interview talking points.** Honest interaction affordances; evidence levels; deterministic eligibility with full thread context; durable jobs versus incremental sync; data minimization and measurable runtime. Earlier visual QA covered the main export flow but missed the shell interaction inventory; this audit corrects the plan, not that implementation yet.

**Owner action.** No credentials needed for this review. Next engineering phase is UI interaction completion and offline boundary verification, followed by guided account setup with explicit authorization before live connections and processing. Full assessment and acceptance criteria are in `requirements-audit.md`.

## Interaction completion — 2026-09-16

**Goal.** Make the visible shell usable and separate drafts from saved results before account setup.

**Decisions and implementation.** Added URL-backed view/run navigation, a keyboard-accessible account menu, functional workspace/profile setup links and breadcrumb, setup/configuration checklist, scenario labels, abortable job polling, and explicit demo parsing guidance. Main modules: `workspace-navigation.ts`, `account-menu.tsx`, `use-job.ts`, `setup-panel.tsx`, `readiness.ts`. URL state selects the view and saved job; API polling loads its immutable criteria/results; a new draft clears the selected run. Configuration checks return booleans, never values or provider health claims.

**Alternatives.** Kept Next routing and one workspace. A new router, fake profile settings, and multitenancy would expand the challenge. Configuration presence is separated from real-service acceptance.

**Validation.** Lint, strict types, 136 unit/integration tests and production build passed. Four isolated API tests passed. Supported-browser checks passed: all shell destinations, account keyboard navigation/Escape, saved-run reload, new draft/result separation, Back, breadcrumb, setup, mobile account navigation; 390px viewport had 390px document/body width. Seven standalone UI tests could not launch Chromium due to the macOS sandbox, so no automated browser pass is claimed yet. Added regression tests for these flows; normal-host CI verification follows.

**Assumptions/risks/security.** Demo remains synthetic; no live calls or credentials. Last ingestion is historical, not a provider health check. Mobile and desktop remain one admin workspace. Recent history is limited to 12 runs. Worker readiness and integration-boundary tests are the next phase.

**Interview talking points.** URL versus transient form state; immutable saved-run ownership; accessible menus and focus; aborting stale requests; configuration versus connectivity versus live acceptance.

**Owner action.** None for this phase. Account setup and live authorization will be requested at the explicit checkpoint.

## Offline integration hardening — 2026-09-16

**Goal.** Reduce mailbox retrieval breadth and prove authentication/interpretation/error boundaries before personal accounts are connected.

**Decisions and implementation.** CRM eligibility now precedes email search, zero eligible contacts make no Gmail calls, and validated primary/secondary emails become bounded sender batches with page/thread deduplication. `executor.ts` still decides exact inclusion after full-thread normalization. `oauth.ts` checks the exact callback and expiration instant. `parser.ts` handles refusal/incomplete/transport outcomes without reflecting provider errors. `job-errors.ts` maps safe recovery messages by stage. `worker-status.ts` and migration 002 expose recent/stale/unseen database heartbeats independently from provider connectivity. Migration startup reads ordered, replay-safe SQL files under the existing PostgreSQL advisory lock.

**Validation.** 174 Vitest tests pass, including OAuth route authentication, encrypted PKCE/state cookie, successful encrypted token persistence, rejected state/expiry/tampering/denial/missing scope/refresh/profile, mocked OpenAI output boundaries, sender batching/empty-candidate behavior, safe errors and worker staleness. Lint, strict types and production build pass. Added a CI workflow and isolated PostgreSQL process smoke test; their execution result is pending at this checkpoint.

**Alternatives and assumptions.** Kept on-demand snapshots and the existing queue rather than adding incremental sync or another queue service. Sender batches minimize unrelated candidate retrieval without changing eligibility; Gmail search is still only a candidate index. Kept complete threads for context, including non-qualifying messages. A heartbeat is a recent observation, not a guarantee of future job success or provider access. CI has read-only repository permissions and no provider credentials.

**Risks/security/remaining work.** Live token grants, HubSpot property/association behavior, model semantics and real runtime remain unverified. CRM snapshot retrieval and thread normalization remain serial/in-memory, suitable only for the measured POC scale until live evidence is collected. Gmail search behavior against actual aliases requires acceptance checks; aliases are passed explicitly because Gmail API search does not perform the UI's alias expansion. No raw errors, tokens, identities or correspondence are added to logs.

**Interview talking points.** Search optimization versus authoritative eligibility; PKCE/state/session roles; schema-valid versus semantically correct model output; worker heartbeat versus job lease; privacy-preserving failure diagnosis.

**Owner action.** No credentials yet. Next checkpoint is CI evidence and precise local account setup. Reference semantics: [Gmail filtering](https://developers.google.com/workspace/gmail/api/guides/filtering), [OpenAI structured output](https://developers.openai.com/api/docs/guides/structured-outputs).


**First Linux CI result.** Run `35141432772` passed lint/types, 174 tests, production build and 9/11 end-to-end tests. Two tests used ambiguous locators: readable message text also appeared inside normalized JSON, and Next's route announcer also had `role=alert`. Scoped the message assertions to exact text and the error assertion to its message. These were test-selection defects, not evidence of a passing full suite. The PostgreSQL smoke check was not reached. Verification is being rerun.

**Second Linux CI result.** Run `35141899203` passed 10/11 end-to-end tests. The remaining assertion required the filename as the entire element text, but the attachment element also includes its size/metadata label. Verified the element in the supported browser and scoped the assertion to the attachment container. No application behavior changed; the complete download/history test and PostgreSQL gate are being rerun.


## CI verification and live setup handoff — 2026-09-16

**Goal.** Close the standalone-browser and native-database evidence gaps, then pause at the owner's requested credential/live-data boundary.

**Implementation and data flow.** `.github/workflows/verify.yml` uses pinned official actions, Node 22, the frozen pnpm lockfile, real Chromium and an isolated PostgreSQL 17 service. `verify-postgres.ts` runs concurrent migrations, persists a synthetic request into the live-mode queue, starts the production web server and a separate test worker process, then verifies authenticated results/CSV. `verify-postgres-worker.ts` injects `DemoCRM`/`DemoEmail` through the existing provider interfaces and forbids HTTP. The ordinary production worker uses the default live providers; no environment flag can silently replace them with mocks.

**Results.** [CI run 35142338893](https://github.com/realined/plum-lending/actions/runs/35142338893) passed at application commit `5c6de87`: lint, strict TypeScript, 174 Vitest tests, production build, 11/11 end-to-end tests in 21.2 seconds, and native PostgreSQL concurrent migrations/separate processes/authentication/4 CSV rows/full-context preservation. The local demo was restarted from the verified build and its setup page shows the embedded worker heartbeat. Final documentation changes do not alter the verified application code.

**Decisions/alternatives.** Used Linux CI because macOS sandbox restrictions prevent standalone Chromium launch. Avoided weakening that sandbox or changing product runtime isolation. A test-only provider composition exercises native storage/worker boundaries without credentials; it is not passed off as real-service proof. Retained the existing stack, on-demand snapshot semantics and single workspace.

**Assumptions, limitations and security.** All test data are synthetic. CI receives no provider secrets and has read-only repository permissions. Owner-local Docker/PostgreSQL has not been configured. Gmail OAuth consent, live HubSpot property/associations, actual model semantics, realistic runtime and real-data CSV remain mandatory. Sequential CRM/thread work and in-memory normalization remain scale limits; incremental sync and large-volume recovery are deferred. Source-only packaging excludes environment files, database contents and browser traces.

**Interview talking points.** Test doubles at provider boundaries; native infrastructure versus live-service acceptance; precise accessible test selectors; search narrowing versus deterministic filtering; heartbeats versus leases and provider health.

**Owner action / current phase.** Choose Docker Desktop with the supplied Compose database or an existing PostgreSQL 17 database (question pending). Follow `docs/live-setup.md` to configure credentials locally. No setup command, account connection, provider consent or live ingestion has been executed by the agent. The pause is required by the owner's explicit collaboration protocol. After configuration, the owner must authorize connection and read-only processing before the procedure in `docs/live-acceptance.md` begins. This phase is prepared, not completed.

## Phase 0 — dedicated live environment inspection, 2026-09-16

**Goal.** Pause feature work, inspect actual implementation, and give exact setup instructions before account creation. The owner requires new dedicated Gmail/free HubSpot accounts and controlled fictional records in the real services; existing personal Gmail accounts are excluded.

**Decisions and implementation.** Added `docs/phase-0-setup-checklist.md`; aligned README, execution checklist, live setup, engineering guidance and interview walkthrough. Traced environment/bootstrap, OAuth/API routes, provider adapters, schema/migrations, worker, normalization, CSV and existing fixture utilities. Preserve the current architecture and read-only runtime. A separate seed writer is explicitly requested, but no write is approved before its preview, dry-run and cleanup review.

**Validation.** Clean starting checkout at `4ff6d5a`. Fresh `pnpm check` passed lint, types, 174 tests and production build. Fresh production API suite: 4 passed in 5.6 seconds. Prior Linux CI proves 7 browser tests and native PostgreSQL/separate processes with injected synthetic providers, not live providers. File-existence checks found no `.env`/`.env.local`; Docker/psql/Desktop were absent from checked locations. No secret contents inspected and no account pages opened during inspection.

**Data flow and alternatives.** Reviewed intent → stored job → CRM snapshot/eligibility → Gmail candidates/full threads → canonical entities → deterministic results → CSV. Existing OAuth is Web client + read-only Gmail; existing CRM auth is a private-app token. Replacing these protocols or silently using mocks would undermine the requested baseline. The body column retains its exact original `Email Body` header and complete thread content.

**Assumptions, limitations and remaining work.** OAuth discovers the primary mailbox; there is no address allowlist environment setting. HubSpot environment bootstrap only validates on export, so a pre-seed credential check is needed. No live seeder exists. Trash-only cleanup would not reset results because Gmail retrieval includes Trash. No API cost arrangement is approved for live OpenAI interpretation. Local PostgreSQL and all real-account acceptance remain open. Phases 1–6 have explicit unchecked gates.

**Security and owner actions.** Owner selects a proposed neutral Gmail address, operates all secrets/security/consent/legal prompts, configures local runtime and enters credentials only in private `.env`. No paid subscriptions, charges, existing personal-mail access, deployment or publication. Later seed writes require separate credentials and approval.

**Interview talking points.** Real services with synthetic records versus provider mocks; runtime read scopes versus seed write scopes; readiness versus authentication versus output correctness; deterministic filtering versus full-thread context; idempotent replay and scoped cleanup.

## Requested official logo — 2026-09-16

**Goal and implementation.** Owner requested a visual exception to the feature pause. Replaced the invented mark in shared `Brand` (sidebar and sign-in) with the unchanged PNG served by the official plumlending.com site. Current corporate site calls it Plum Infinity; the workspace retains its Plum Lending name. Recorded source/ownership in `public/brand/README.md`, adjusted responsive CSS, and added asset/documentation to the source manifest.

**Choice and data flow.** Serve the original asset locally through Next Image. This preserves the actual artwork and avoids runtime corporate-site requests; do not redraw or generate an imitation. No provider or domain behavior changes. Validation is recorded after the final build/visual check below.

**Logo validation and limits.** After the visual change, `pnpm check` passed again: lint, strict types, 174 tests (2.09 seconds), production build. Restarted the local demo and verified the official image loaded in the built-in browser. Desktop and 800px compact sidebar display the logo; at 390px the existing mobile navigation intentionally hides the sidebar. Document widths matched 800px/390px with no horizontal overflow. The shared sign-in component receives the same asset but was not separately exercised in live mode. No new tests were added for this static visual change. No provider configuration or live data was used. Updated files remain local; nothing was pushed or published in this phase.

## Phase 1 — dedicated Gmail signup handoff, 2026-09-16

**Goal and decision.** Owner proposed a dedicated challenge-lab address. Opened Google account creation in the built-in browser; address availability and account creation remain unverified. HubSpot has not been created and will use the new mailbox after confirmation.

**Validation and owner action.** Verified the Google signup page reached the name-entry step. Paused before entering personal information, following the owner's manual-handoff requirement for personal details, username, passwords, security prompts and legal acceptance. Left the signup tab available. No credentials read, screenshots captured, accounts connected, live records processed or code changed. No test rerun is needed for this documentation-only checkpoint.

**Remaining work and interview points.** Confirm the dedicated mailbox after owner signup, then configure the separate OAuth client and free HubSpot account. Account creation is distinct from application OAuth consent; email account ownership is distinct from provider authorization; a proposed username is not a verified mailbox. Phase 1 remains incomplete.

**Phase 1 verification blocker.** Owner reports that Google rejected the phone number because it has already been used with other accounts. Google's official account-verification help directs users with number-use-limit errors to another number, including a friend/family number with permission: https://support.google.com/accounts/answer/114129?hl=en. No number or code was requested, inspected or recorded. Account creation remains unverified; await owner completion through Google's supported verification process. No personal mailbox substitution or HubSpot creation.

## Test-only access for the owner-selected existing mailbox — 2026-09-16

**Goal and revised assumption.** The owner approved reusing the existing job-search/BenTech Gmail account after new-account phone verification was blocked. It contains unrelated correspondence that must not appear in the app. New mailbox creation is superseded; unrelated mail and all other personal accounts remain outside authorization. No live account connection has occurred.

**Decisions and implementation.** Added a default `seed-only` runtime boundary while retaining the current stack, OAuth flow and deterministic domain rules. `gmail-access.ts` validates the expected account and privately loads the seeder receipt; `gmail-seed-policy.ts` validates unique insertion IDs/namespace; `gmail.ts` narrows label/search fields, verifies thread membership without content, and fetches exact message IDs individually. OAuth checks the actual profile against the configured mailbox. `DataBoundaryError` stops the executor and prevents worker publication. Updated setup guidance and documented the new supported `.env` fields. Saved the selected account only in ignored local configuration; it is absent from submission source.

**Data movement and alternatives.** Query → existing reviewed specification → CRM eligibility → seed-label ID search → receipt membership check → exact approved message bodies → normalization/filtering → stored preview/CSV. A label or subject prefix alone can include unrelated replies. Fetching whole thread bodies then hiding real messages would already have retrieved them and would misrepresent completeness. Exact insertion IDs plus content-free checks preserve complete approved threads and reject mixed ones. A reply arriving mid-read cannot trigger an unapproved body request. Mailbox-wide incremental history is disabled in protected mode.

**Validation.** Added 30 tests, bringing the suite to 204 across seven files. Synthetic tests cover incorrect/missing account, absent/malformed receipts, accidental labels, unknown/duplicate/missing IDs, mixed threads, mid-read private replies, marker mismatch, caller mutation, safe errors and complete synthetic context through CSV. A worker test proves a later boundary failure publishes zero canonical records and zero export rows, even after an earlier valid thread was read. Lint, strict types, production build and four API tests passed before the final guidance-copy adjustment. The built-in browser verified the new data-scope setup item. A JSX apostrophe lint issue in that final copy was corrected; final rerun results follow below. No actual correspondence, secrets or provider account pages were inspected in this implementation checkpoint.

**Limitations and remaining work.** This is application-enforced minimization, not a Google label-scoped permission. The OAuth token remains mailbox-wide and the local receipt/configuration are trusted inputs. The explicit `mailbox` operator mode must never be used for this account. Prior stored data is not retrospectively scrubbed; no live records have entered this project, and live acceptance must start with a fresh database. The live seeder, actual OAuth/HubSpot connections, provider response behavior, local PostgreSQL and CSV acceptance remain open. Earlier full browser/native PostgreSQL CI predates this change; publishing remains paused. The receipt must come from the seeder's actual insertion responses, not hand-picked real messages.

**Owner action and interview talking points.** Next, sign into Google Cloud with the approved account; handle passwords/security/consent/legal acceptance privately. The separate seed-write preview and approval remain required. Explain OAuth versus retrieval scope; labels versus immutable insertion IDs; check-then-fetch races; fatal privacy failures versus recoverable outages; complete approved threads versus silently truncated mixed threads. Full design, tests and limits are in `docs/mailbox-isolation.md`.

**Final verification.** After correcting the guidance text, lint, strict types, all 204 tests (2.07 seconds), production build and all four production API tests (5.6 seconds) passed. Rebuilt/restarted the local demo and verified the final test-only guidance in the built-in browser. Source-manifest checks confirm private account selection/runtime files are excluded and the approved real mailbox identity is absent from submission source. No push, deployment, live provider read or seed write occurred.

## Phase 2 — Google Cloud setup started, 2026-09-16

**Goal.** Configure a dedicated project and Web OAuth client for the approved account using the existing exact callback and `gmail.readonly` scope. Complete only after real app consent/callback/profile validation; the seed receipt remains a separate ingestion gate.

**Progress and handoff.** Opened Google's account chooser in the built-in browser, verified the approved account was the offered identity, and selected it to reach Google Cloud. Account passwords/security prompts, legal acceptance, credential entry and OAuth consent remain owner-operated. No Gmail correspondence is accessed by this step. Project/client creation and live connection verification remain pending.

**Current manual checkpoint.** Google Cloud displays its welcome dialog and Terms of Service for the approved account. Stopped before checking agreement or clicking Agree and continue, as the owner explicitly reserves legal acceptance. No billing/trial enrollment or cloud project creation occurred. Owner should review the terms, confirm country and continue only if they agree; any payment/trial prompt is a separate stop.


**Cloud/API checkpoint.** Observed that the owner completed the Cloud welcome terms. Created the dedicated neutral project `BenTech Lending Lab`; the console verified Gmail API status **Enabled**. A transient API-page load failure recovered on one retry. Prepared the OAuth registration with External/Testing audience and the approved account as support/developer contact. Registration is not submitted: paused at the explicit Google API Services User Data Policy agreement for owner review, followed by Continue/Create. No test user, client credential or Gmail grant exists yet.

**Local configuration.** Ran the existing `scripts/prepare-live.ts` through `pnpm setup:live`. It created `.env` directly with generated administrator/session/encryption values; no values were returned. Metadata-only verification confirmed mode `0600`, Git ignored and untracked. Provider credentials remain unfilled. Confirmed no Docker Desktop, Postgres.app or Homebrew in the inspected standard locations. The existing running web process remains explicitly in demo mode; PostgreSQL and the separate live worker are not configured.

**Architecture and alternatives.** The project enables API use, the Web client will identify the local server, and the eventual owner consent will authorize mailbox access; these are separate steps. Retained the existing server-side authorization-code/PKCE flow and exact callback. A service-account key or the chat Gmail connector would not exercise the required app OAuth flow. Normal runtime stays read-only; a separate seed authorization and reviewed dry-run remain prerequisites to dataset writes.

**Validation and remaining work.** Console observation proves project creation/API enablement only. File metadata proves private environment creation only. No code changed, so the preceding 204 unit/integration tests, four API tests, lint/types/build remain the latest application evidence, not newly rerun results. HubSpot setup, PostgreSQL, OAuth client/test user/scope, real app connection, guarded seeder and live CSV acceptance remain pending. No correspondence, provider token, seeded record, billing enrollment, push or deployment was accessed or created.

**Owner action and interview concepts.** Review and accept the API policy only if agreed, then complete Continue/Create and report completion. Later copy Google client credentials directly into the existing private `.env` fields `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`; never send them through chat. Explain project/API enablement versus OAuth consent; Web callback plus PKCE; Testing users versus production publication; OAuth permission versus seed-only retrieval; read-only runtime versus separate seed writers.


**Phase 2 registration/scope checkpoint.** The owner completed the API-policy agreement; the console confirmed OAuth configuration created. Verified External audience and Testing publishing status, saved the approved mailbox as the sole test user (1 test, 0 other), and saved only `https://www.googleapis.com/auth/gmail.readonly` under Data Access. Prepared, but did not submit, the Web application client `BenTech Lending Lab Local Web` with exact redirect `http://localhost:3000/api/connections/gmail/callback`. JavaScript origins are empty because this app exchanges authorization codes on the server. The enabled Create control and form values were verified before handoff.

**Configuration and owner handoff.** Populated only the approved expected mailbox, `seed-only` mode and insertion-receipt path in the existing ignored local `.env`; all credential fields were preserved and no values displayed. The owner now clicks Create and privately copies Client ID/secret into `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`, saves the file, closes the credential dialog and returns to Overview before reporting completion. Tool observation stops before credential creation. Client creation and credential entry are not marked verified merely because instructions were delivered.

**Validation, data flow and limits.** Verified the saved test-user count and the Data Access success notification through the actual console. Reconfirmed the scope and callback against `src/server/oauth.ts` and `.env.example`. No application source changed; prior 204 tests/four API tests remain the latest regression results. Source diff whitespace validation passes. The configured client will identify the server, owner consent will grant read access, and the expected-mailbox/seed receipt guards will restrict ingestion. None of those later runtime steps has passed yet. HubSpot, local PostgreSQL, the separate seed writer and real CSV acceptance remain open; current app data remain mocked. No credentials or correspondence were read, no records seeded and nothing published.

**Interview concepts.** Declared scopes versus granted scopes; Testing allowlist versus mailbox permission; server redirect URI versus browser JavaScript origin; client identity versus user authorization; application-level seed isolation despite mailbox-wide OAuth. A desktop client or service account is not selected because neither matches the implemented server callback and user authorization-code flow.


## Phase 3 — free CRM setup and pre-seed authentication check, 2026-09-16

**Goal.** Finish the owner's free HubSpot onboarding and prove read-only API access before any synthetic record writes. Google client credentials have been saved by the owner; non-disclosing checks confirmed both fields are populated and the exact callback, approved mailbox, seed-only policy and `0600`/Git-ignore protections are correct. This is configuration evidence, not successful OAuth authentication.

**Implementation and data flow.** Added `src/server/hubspot-check.ts`, thin `scripts/check-hubspot.ts`, `pnpm hubspot:check`, and focused tests. Explicit invocation loads the local environment and reuses the existing adapter's five GET validations for contacts, Sponsor property, companies, deals and pipelines. Object reads are limited to one record; response content is discarded. No database, Gmail, paid model, persisted connection or record write is involved. All reported outcomes are fixed text, including unexpected errors. The check rejects non-live mode, missing/placeholder credentials and invalid property names before network access.

**Decisions and alternatives.** This independent setup command avoids requiring a Gmail insertion receipt or an export job just to test a HubSpot token. It preserves private-app authentication and the existing provider behavior. The UI token form remains an alternative for validating and persisting the app connection, but the owner selected local environment entry. The check deliberately does not claim to prove portal identity, associations, actual ingestion or connected status in the application.

**Validation.** Final `APP_MODE=demo pnpm check` passes: ESLint, strict TypeScript, 218 tests across eight files (2.06 seconds), production build. Fourteen added synthetic tests cover no-network input rejection, the bounded read-only request set, discarded response content, HTTP 401/403/404, malformed JSON and secret-bearing unexpected error stand-ins. The real CLI invoked with `APP_MODE=demo` refused with exit code 1 as intended. It was not run against HubSpot. Two intermediate type-check failures from Next's ambient environment typing were fixed with a narrow explicit configuration object before the successful final suite. No new admin UI behavior changed; the preceding four API and earlier browser tests remain historical evidence. Rebuilt/restarted the demo explicitly with its embedded worker, preserving offline availability after creation of the live environment file.

**Account checkpoint.** The owner completed private signup steps and supplied an owned website. Guided the visible wizard: skipped automatic Google email/contact sync, selected manual setup and the sales-pipeline use case, then dismissed the optional tour. Observed an empty Deals board with default Closed Won and Closed Lost stages. The portal selection is recorded only in ignored owner-local configuration and remains marked unverified for API authentication. No paid trial, mailbox import, seed write or publication was selected. Contact property and private-app configuration are the next steps.

**Limits, security and interview points.** Real-service authorization, Sponsor schema, associations and end-to-end correctness remain unverified. Gmail/HubSpot data inside the app are still mocked. The runtime should have read scopes; a separate approved seed writer needs separate write authorization. Explain configuration versus authentication versus ingestion; endpoint permissions versus account identity; schema internal names versus display labels; bounded preflight reads versus full snapshots; fixed diagnostic categories versus leaking provider bodies. Owner action will be private-app credential creation/entry after the read scopes are prepared; passwords, consent and legal prompts stay owner-operated.

**Schema and permission-form checkpoint.** Created the Contact information property labeled Contact type, verified internal name `contact_type`, and selected single-line text without unique-value enforcement or AI autofill. The property appears in the saved property list; no contact records were created. Prepared `BenTech Lending Lab Reader` and verified exactly five selected scopes: contacts, companies and deals object reads, plus contact and deal schema reads. The Create app control is enabled, but has not been clicked. The token and API grant are not yet created or verified.

**Tradeoff and owner handoff.** HubSpot's current wizard labels private apps as legacy and recommends Service Keys, while stating legacy functionality remains available with limited future support. Retained the implemented private-app flow to avoid changing authentication during the baseline; future migration remains technical debt. The owner must click Create app, complete any final confirmation, privately save the token into the existing `HUBSPOT_PRIVATE_APP_TOKEN` field, and close the token screen before observations resume. Tool access stops before credential issuance, following the owner's protocol. No write permissions, seed records, inbox import, paid plan or publication were added.

**Validation and remaining work.** The saved property and final five-scope summary are directly observed configuration evidence. The preceding 218-test/lint/type/build results remain current; documentation-only checkpoint updates do not require another application suite. Live HubSpot token validation, actual Gmail OAuth, native PostgreSQL, separate approved seeding and live CSV acceptance remain open. Interview concepts: schema internal identifiers; read-only runtime versus seed writers; configuration versus authentication versus ingestion; fixed safe diagnostics; compatibility versus migration debt.


**Live authentication result.** The owner reported the HubSpot token saved and its display closed. Non-disclosing checks verified a populated token, live configuration, exact Sponsor mapping, and private/ignored/untracked environment file. Ran the actual `pnpm hubspot:check`: exit 0, all five read checks passed (contacts, Sponsor property, companies, deals, pipelines). This is live authentication evidence using the application adapter. Record content was discarded; no writes, persisted connection, Gmail access, paid model call or credential output occurred. Phase 3 authentication is complete; portal identity, association behavior, application connection state and end-to-end ingestion remain open. The previously passing 218 tests/lint/types/build are unchanged.

**Local runtime handoff.** Verified Apple silicon and no Docker CLI/Desktop, PostgreSQL CLI/Postgres.app or Homebrew in the inspected standard locations. Retained the existing `docker-compose.yml` PostgreSQL 17 service on loopback instead of substituting the single-process demo database. Opened the official Mac Docker installation instructions and requested owner installation/startup, including their own review of the license and any administrator password prompts. No installation, legal acceptance, billing, live database migration or server mode switch has occurred. After Docker is ready, verify database health, migrations and separate web/worker startup; actual Gmail consent remains owner-operated. The model API no-charges constraint and separately approved seed writes remain unresolved gates.


## Local runtime and real connection preparation — 2026-09-16

**Goal and decisions.** Start the existing PostgreSQL service and separate web/worker processes so the real application can persist encrypted provider connections and durable jobs. The owner installed and launched Docker Desktop. Retained the checked-in PostgreSQL 17 Compose service on `127.0.0.1:5432`; did not substitute the demo-only single-process PGlite database or introduce a hosted service. No application source changed in this checkpoint.

**Implementation and data flow.** Ran Compose with a bounded health wait, then `pnpm db:migrate`. Docker created a fresh project volume; database counts initially confirmed zero connections, jobs, contacts and messages. Stopped the known demo server and started the existing production build with live mode and embedded worker disabled, then started the separate worker. Both use the same private environment and PostgreSQL. Explicitly invoked existing `src/server/connections.ts` `connectHubSpot` with the environment token; it repeated the five bounded read validations and saved the encrypted token via `src/server/crypto.ts`/`database.ts`. This operator action is separate from the side-effect-free `hubspot:check` CLI. No export or seed writes occurred.

**Validation results.** Docker server 29.8.0 responded. PostgreSQL 17.11 is healthy and bound to loopback; migrations 1 and 2 and 16 public tables are present. Live worker heartbeat is fresh; job count remains zero. Unauthenticated `/api/status` returns 401. After the owner privately entered the administrator password, the actual UI shows Live data, HubSpot Connected, no completed ingestion, and heartbeat received from a separate process. A count/boolean-only query confirms saved HubSpot connection with a populated encrypted token and generation; contacts and messages remain zero. Existing 218-test/lint/type/build results still apply; no source changes justify a repeat suite.

**Known limits and security.** `pnpm config:check` exits 1 because `OPENAI_API_KEY` is absent. Full interpretation readiness is not claimed; no metered model calls or new billing are authorized. Gmail OAuth is still pending owner consent, and the seed receipt does not exist, so protected ingestion remains blocked. HubSpot API identity/associations and live output correctness still need testing. Secrets were consumed privately by the configured server function, not displayed; only encrypted token storage and safe fixed diagnostics were used. The app now runs in live mode with an empty dataset, while offline mocks remain available for tests/demo. No deployment or publication occurred.

**Interview points and owner action.** Explain separate worker versus embedded demo execution; transactional/advisory-locked migrations; database heartbeat versus provider access; authentication versus encrypted connection persistence; OAuth scope versus seed-only retrieval restrictions. Owner administrator sign-in is complete. Owner is now asked to initiate Gmail Connect and complete Google account selection/consent privately, then return to the application for callback verification.


**Gmail live completion checkpoint.** The owner completed the actual Gmail Connect/consent flow and returned to the app. The live UI shows two Connected providers, two No completed ingestion indicators and a separate worker heartbeat. A boolean-only database check confirms Gmail connected, approved account equality, populated encrypted token and connection generation. Counts remain zero for contacts, companies, deals, threads, messages and export jobs. This verifies Phase 2 real OAuth and the local runtime/connection stage; it does not verify message retrieval or CSV results. No account address, token, authorization code, callback URL or email content was recorded in evidence.

## Phase 4a — deterministic seed proposal and MIME preview, 2026-09-16

**Goal.** Make the proposed controlled live dataset concrete before broader writer credentials or record creation. Preserve normal read-only app grants and give the owner an inspectable plan with expected outcomes.

**Decisions and implementation.** Added `src/seed/plan.ts`, `src/seed/mime.ts`, `scripts/seed-preview.ts`, `tests/seed-plan.test.ts` and `pnpm seed:preview`. The plan freezes reference dates, namespaces every record identity, includes seven contacts/companies, three deals, six threads and nine messages, and emits a stable SHA-256 digest. The MIME generator supplies exact reply headers, shared subjects, plain/HTML alternatives, synthetic CC and an attachment. A changed plan or unsafe input is rejected. The command supports only an offline dry-run and imports no provider/database/environment loader. Registered source files in the explicit archive manifest.

**Data flow and validation.** Fixed fictional scenario definitions → frozen domain specification and expected outcomes → RFC message generation → offline preview. Fifteen focused tests pass, including actual MIME parsing and a simulated Gmail projection through the real normalizer, deterministic executor and CSV generator. Expected result is two included contacts, two rows and five full-thread messages. Cedar's recent final reply is preserved outside the qualifying window. A full regression run initially caught the mailparser CC union type in the test; it was corrected before the rerun. Final full-suite results will be recorded below.

**Alternatives, permission decision and remaining work.** Stable RFC Message-ID alone is not a replay guarantee; the future writer needs a durable journal of actual insertion IDs and a fail-closed uncertain-write policy. Gmail `modify` permits reversible Trash cleanup but also technically permits sending. Proposed narrower `insert`/`readonly`/`labels` grants avoid sending capability; cleanup can remove the dedicated label and disable the app receipt, but synthetic messages remain in Gmail. The owner has been asked to choose this consequential permission/cleanup policy before that part of the writer is implemented. No grant expansion or live write has occurred. The live writer, separate authorization, account preflight, replay/cleanup implementation and actual insertion receipt remain pending.

**Security and interview points.** All preview identities/content are fictional and use example.test. The real mailbox is not embedded in source or the preview. The app's live connections remain read-only and the receipt gate blocks ingestion. Explain frozen expected outcomes versus moving time windows; RFC threading versus provider thread IDs; insertion versus sending; a plan digest versus write authorization; successful replay versus uncertain-create recovery. See `docs/seed-plan.md` and the generated offline preview for owner review.


## Phase 4b — guarded seed writer and authorization, 2026-09-16

**Goal and decision.** Implement the reviewed dataset workflow independently from the read-only application. The owner selected narrow Gmail insert/read/labels authorization; cleanup retains synthetic messages. That permission-design choice does not authorize live writes. Normal application grants stay read-only.

**Implementation and data flow.** `src/seed/{cli,oauth,private-files,transport,writer}.ts` add private credential-template setup, a loopback OAuth helper with state/cookie/PKCE and exact scopes, encrypted short-lived authorization, an endpoint allowlist, account/schema/pipeline preflight, an exclusive writer lock, durable create journal, replay, receipt publication and scoped cleanup. Approved plan → verified accounts → journaled provider creates → metadata/association/thread membership checks → private insertion receipt → normal app's protected reader. `docs/seed-plan.md` contains the exact setup and review procedure. No seed command runs at application startup.

**Validation.** Final `APP_MODE=demo pnpm check` exited 0: ESLint, strict TypeScript, 253 tests across nine files (3.63 seconds), and production build. Thirty-five focused seed tests cover deterministic records, MIME round-trips, real normalization/filter/CSV with simulated provider data, disabled/account/scope/digest rejection, successful replay without duplicate creates, uncertain creates, mixed threads, narrow cleanup, endpoint restrictions, encrypted authorization and an actual local callback rejecting wrong state before a Google request. Earlier API/browser/CI results remain historical and were not rerun for this CLI-only addition. No live writer call, seeded record or ingestion has occurred.

**Alternatives and security.** A broad Gmail modify grant was not selected; insert/read/labels cannot send or trash messages. A separate client and recommended separate Cloud project isolate seed consent. The helper verifies a different client ID, not Cloud project identity. No refresh token is stored for the writer. Provider writes are not blindly retried: a pending uncertain create requires manual inspection. Fixed safe diagnostics avoid tokens, identities and provider response bodies. The operation journal and credentials stay private and ignored.

**Limitations and remaining work.** Writer credentials/consent and live preflight/apply/replay are pending. Stop web/worker during apply or cleanup because their export queue does not share the writer lock. Cleanup removes the label and receipt and archives only verified journaled HubSpot objects; it retains Gmail messages and does not erase already derived app exports. Apply after cleanup is blocked; reset/recovery is manual. OpenAI credentials and permission for metered testing remain unresolved. Core live acceptance is still incomplete.

**Interview talking points.** Read-only runtime versus a separate writer; insertion versus sending; a digest versus human authorization; durable known-success replay versus uncertain outcomes; full-thread membership checks before receipt publication.

## Deadline and presentation plan — 2026-09-16

**Goal.** The owner presents Thursday, September 17 at 4:00 PM Eastern. Prioritize verified live behavior and interview readiness. Target live acceptance by 1:00 PM, code freeze by 2:00 PM and rehearsal before 3:00 PM, subject to credential/consent and provider issues. These targets are not completion claims.

**Decisions and remaining work.** No new product surface or technology change. Finish writer setup, reviewed seed approval, actual provider ingestion/associations, natural-language evaluation, deterministic expected-versus-actual results, full-thread CSV and repeat-run evidence. Resolve the no-spending gate before OpenAI calls. Then refresh docs/source package, review the final diff, prepare an honest offline fallback, and rehearse. Publication remains paused until explicitly reauthorized. Optional polish is considered only after the baseline report.

**Presentation talking points.** Explain the business outcome, demonstrate why each case is included/excluded, distinguish AI interpretation from deterministic decisions, show context outside the matching window, and state measured behavior and production limitations accurately. A strong small vertical slice is the delivery priority.

**Checkpoint verification.** Created the empty `.env.seed` template with mode 0600; both environment files are ignored and untracked. The disabled seeder CLI refused preflight with exit 1 and `SEED_ENABLE_FLAG_REQUIRED`, without provider access. All 118 source-manifest files exist; a non-disclosing scan found no configured private credential or mailbox values in them. Diff whitespace check passed. The production web server restarted successfully; a fresh UI check was blocked by the browser tool's handling of an existing connection-error page, so earlier live connection UI evidence is retained rather than claiming a new pass.


## Phase 4c — separate live writer configuration, 2026-09-16

**Goal and status.** Prepare the live seed grants so the reviewed fictional corpus can be inserted and real ingestion tested. A presence-only check confirmed all three writer credential fields empty, enable flag false, and no writer token, state or insertion receipt. `pnpm seed:preview` exited 0 and reproduced the reviewed digest and seven-case outcome: 7 contacts, 7 companies, 3 deals, 6 threads, 9 messages, 2 expected CSV rows. No seed provider call or write has occurred.

**Configuration prepared.** In the approved HubSpot lab, prepared a new BenTech Lending Lab Seed Writer private-app form with exactly eight scopes: contacts/companies/deals read and write, and contacts/deals schema read. The app is not yet created; the owner must complete Create app, save its token directly into `SEED_HUBSPOT_PRIVATE_APP_TOKEN` in private `.env.seed`, and close the token screen. The normal reader token remains unchanged.

**Google configuration.** Created the separate BenTech Lending Lab Seed Cloud project and directly verified Gmail API Enabled. Prepared OAuth branding using the approved account and External/Testing audience. Stopped at the unchecked Google API Services User Data Policy agreement for owner review/acceptance. Registration, test-user list, three writer scopes, Web client, private credentials and consent remain pending. No billing, trial or metered model request was enabled.

**Remaining work and interview points.** After both manual handoffs, finish the Google writer registration/client and owner consent, run live preflight, present the concrete write summary and obtain execution approval, then seed and verify replay. The normal app remains read-only; no live messages have been ingested. Explain API enablement versus user authorization, separate write/read grants, provider identifiers versus fixture identifiers, and expected results versus measured live results. Earlier 253-test/lint/type/build evidence is unchanged; no application source changed in this setup checkpoint.

**Writer credential/configuration handoff update.** The owner reported creating the HubSpot writer token and Google registration. Initial presence-only inspection found the writer token absent from `.env.seed`; the owner corrected an accidental edit to the other environment file. Recheck confirms the writer token present, mode 0600, and different reader/writer tokens. A local boolean-only comparison confirms the normal `.env` HubSpot reader still exactly matches the encrypted, previously verified database connection. No secret values were output.

Google registration creation was directly confirmed. Verified External/Testing with exactly one approved test user; saved exactly `gmail.insert`, `gmail.readonly`, and `gmail.labels` and observed the success notification. Prepared a Web application named BenTech Lending Lab Seed Writer with the sole redirect `http://127.0.0.1:3001/seed/callback` and no JavaScript origins. Stopped before client creation for the owner to save credentials privately in `.env.seed`, enable the explicit seed workflow and close the credential dialog. Client creation/consent, full preflight and all live seed records remain unverified. No application source changes or new regression result are claimed.

**Separate writer readiness and first live check.** Owner reports Google writer client credentials saved and the credential dialog closed. Presence-only checks pass for all three writer fields, distinct reader/writer clients and HubSpot tokens, mode 0600 and Git-ignore protection. The persistent seed-enable flag remains false; the explicit `seed:auth` process was enabled only for that invocation. The loopback consent helper is running with a 10-minute window and awaits owner consent; no authorization URL, secret or callback code is logged.

Using the separate HubSpot writer token, the real private-app token-info check confirms the approved portal and all eight required scopes. The existing bounded HubSpot adapter checks also pass for contacts, Sponsor schema, companies, deals and pipelines. Output contained only booleans and zero records written. This proves writer identity/permissions/read access; actual writes, associations, full preflight and replay remain unverified. The current owner handoff is the separate Gmail seed consent.

**Gmail seed authorization verified; preflight stopped safely.** The owner completed the separate seed consent. The helper exited 0 with its fixed success message; boolean-only checks confirm encrypted short-lived authorization saved, mode 0600 and unexpired. No dataset writes occurred. The combined `seed:preflight` reached the CRM inventory check and exited 1 with `SEED_UNTRACKED_OR_CHANGED_CRM_RECORD`. The earlier identity, writer-scope, Sponsor-schema and unambiguous-pipeline checks completed before that guard. A bounded IDs-only CRM count query found 2 existing contacts, 1 company and 1 deal; contents and IDs were not output.

**Owner decision required.** Asked whether all four existing records are disposable onboarding samples authorized for archival, or must be preserved. No archival or automatic adoption is authorized yet; no records were changed. Preserve the strict empty-baseline guard until this concrete conflict is resolved. Live seeding, replay, ingestion and CSV acceptance remain pending.

**Approved sample archival and live insertion attempt.** The owner explicitly authorized archiving all four disposable onboarding records. Stopped web/worker, verified the approved account, exact 2-contact/1-company/1-deal inventory and no active exports; privately journaled the exact IDs and archived only those records. Verified all three active inventories empty. Combined live preflight then passed with the reviewed plan digest. Proceeded under the owner's instruction to populate the reviewed synthetic dataset.

The writer created and journaled the first company, then stopped on `SEED_PROVIDER_REJECTED` at `contacts:cedar`. No Gmail label/messages or receipt exist. Before a diagnostic retry, verified zero contacts and the exact pending stage; repeated the identical approved contact request once and inspected only allowlisted status/category/property codes. HubSpot returned HTTP 400, VALIDATION_ERROR, INVALID_EMAIL for email. The pending journal is preserved; no blind replay or clearing occurred.

**Proposed compatibility decision, awaiting owner.** Replace only the live seed contact addresses and their matching Gmail participants with namespaced @example.com addresses, an IANA-reserved documentation domain; preserve company records, RFC IDs, cases and expected outcomes. Disable the lab's automatic company creation/association to prevent unreviewed records. This changes the frozen dataset and account setting, so explicit owner approval is pending. Regenerate the plan and carefully reconcile the one known company only after approval. Live end-to-end acceptance remains open.

## Phase 4 completion — approved live seed and replay, 2026-09-16

**Goal.** Populate real services with the approved fictional truth set while preserving unrelated-mail isolation and normal read-only grants.

**Decisions and implementation.** Owner approved namespaced reserved @example.com contact addresses after HubSpot rejected .test, plus disabling automatic company creation/association. Updated `src/seed/plan.ts`, the domain expectations in `tests/seed-plan.test.ts`, `AGENTS.md` and the seed plan. Kept company domains/RFC IDs/CC placeholders unchanged. Verified the single created company against its private journal and verified no contact existed before reconciling the pending rejected operation. Broad adoption, blind retries and dataset redesign were rejected.

**Validation.** Company automation disabled and persisted. Live preflight/apply passed: 7 contacts, 7 companies, 3 deals, reviewed associations, 6 threads and 9 messages. Receipt published after membership checks. A second apply reused all journaled IDs; no duplicate creates or pending operation. After the code change, 35 focused tests and full lint/types/253 tests/production build passed.

**Assumptions/security/remaining work.** Only the approved account and fictional records are in scope. No email was sent. Writer credentials and receipt remain private; enable flag remains false except explicit commands. Runtime readers retain read-only grants. Cleanup is implemented/contract-tested but not live-tested; it retains Gmail messages and requires a separate approved operation. Phase 4 live creation/replay is complete.

**Interview points.** Contract tests versus real-provider constraints; journaled partial-create recovery; separate writer/read permissions; insertion versus sending; exact receipt versus a label-only privacy boundary.

## Phase 5 checkpoint — live reader, repeatability and downloaded CSV, 2026-09-16

**Goal and data flow.** Verify the existing approved specification independently of the unresolved AI credential gate: saved typed specification → durable queue → separate live worker → real read-only HubSpot/Gmail adapters → deterministic eligibility → complete normalized threads → stored results → authenticated CSV download. The saved source explicitly says integration test/no AI. This is not a substitute for the final natural-language journey.

**Implementation and important modules.** No reader/domain architecture change. Exercised `src/server/repository.ts`, `src/server/worker.ts`, live providers, domain normalization/eligibility and `src/domain/csv.ts`; generated count-only evidence privately and refreshed handoff documents.

**Actual validation.** Two jobs succeeded in 6.862 and 7.402 seconds including queue time. Each scanned 7 contacts: 2 included, 5 excluded, 2 threads, 5 messages, 0 failures. Exclusions match the seven-case truth set: 1 non-Sponsor, 1 Closed Won, 3 without qualifying inbound email. Four-message thread preserves the out-of-window recent reply, CC and attachment metadata; UI inspected. Actual UI CSV download succeeded and the private downloaded file parses with 8 exact headers, 2 rows, 5 normalized messages and populated subjects/bodies. Same-request enqueue returns the same job. Canonical counts remain 7 contacts/7 companies/3 deals/2 threads/5 messages.

**Repeat comparison finding.** Initial byte-equality assertion failed because Gmail changed one opaque attachment retrieval ID. A field-level comparison confirmed every other field matches. Semantic repeatability and entity deduplication pass; raw CSV bytes differ and are not claimed identical. Retaining provider provenance is appropriate; stripping it merely to satisfy this check was rejected.

**Mock/live status, privacy and limits.** Reader and writer positive paths are verified against real accounts. Offline adversarial privacy/cleanup tests remain mocks. Live files stay outside source control; output evidence includes counts/booleans only. No raw CSV or provider identifiers copied into docs. Dataset is intentionally small, so timing is not a scale benchmark. Continuous sync, worker-restart live recovery and live cleanup remain unverified/deferred.

**Remaining work and owner action.** OpenAI key is absent and no paid call occurred. A pending owner question requests permission up to $1 and private key entry. Then run actual interpretation semantics, reviewed UI execution and final acceptance/rehearsal. Publication remains paused.

**Interview points.** Interpretation versus execution; deterministic exclusion reasons; matching-window versus full-thread context; durable queue/idempotent persistence; semantic stability versus provider-handle volatility.

## Phase 5 AI setup — metered-use authorization

The owner explicitly approved up to $1 total API usage for interpretation acceptance tests and presentation rehearsal. This supersedes only the previous no-model-spending restriction; it does not authorize a larger prepaid purchase, subscription, automatic recharge, publication or deployment. Opened the official Platform sign-in and handed off the password/verification step. The owner will create and privately save the API key; no secret should enter chat or observed browser output. No API call or charge has occurred at this checkpoint.

Retain `gpt-4.1-mini`, Responses structured output, query-only input, `store: false`, 1,000 maximum output tokens and one SDK retry. Official model pricing checked: $0.40 input and $1.60 output per million tokens. Plan at most 10 interpretation invocations (up to 20 HTTP attempts including retries) for the initial five-case evaluation, UI workflow and rehearsal. Check request size and estimated worst-case cost before calls; keep usage evidence count-only. This is an operator test budget, not an implemented product-wide billing cap.

Done criteria remain: real-model semantic cases pass, reviewed UI run succeeds using live providers, downloaded CSV matches the truth set, and the owner can rehearse the explanation. Credential entry and any necessary billing/terms steps remain owner-operated under the collaboration protocol.

**API account handoff.** Owner completed sign-in. The official billing UI shows zero API credit, an existing automatic-reload setting, and a $5 minimum manual credit purchase. No purchase or billing setting was changed. Asked the owner to decide on the larger prepaid purchase and disable automatic reload before funded tests; the approved test-usage ceiling remains $1.

Prepared, but did not submit, a separate named API key in the existing Default project with seven-day expiration and Restricted Responses Write (including read) permissions; all other capability groups remain None. Owner must create the key, paste it directly into the existing private `.env` field `OPENAI_API_KEY`, save, close the secret dialog and report completion. No API request or new secret was generated/read by the agent. This checkpoint changes documentation only; prior 253-test/build evidence remains the current implementation validation.

## Phase 5 complete — real AI and browser acceptance, 2026-09-16 Eastern

**Goal and decisions.** Complete the actual natural-language path without changing architecture. Owner privately saved the key, confirmed credit funded/automatic reload off, and authorized up to $1 total usage. Presence-only checks verified the key, expected model, live/seed-only configuration, mode 0600 and Git-ignore protection. Restarted web to load the key. Retained the existing gpt-4.1-mini Responses parser, strict schema, query-only input and deterministic executor.

**Data flow and files.** `parser.ts` produced validated intent/spec; the browser displayed source/assumptions before `api.ts` saved the durable job. The separate `worker.ts` read real HubSpot and protected Gmail data; domain executor/normalizer/CSV and repository published the result. No feature code changed in this phase. Updated acceptance/setup/checklist/README/demo/interview documentation.

**Actual results.** All five real semantic cases pass: two equivalent 24-minus-3 requests, one 12-minus-2 request, and rejection of unsupported amount/outbound conditions. Five HTTP calls consumed 1,362 input and 228 output tokens, estimated $0.0009096. The browser added one real interpretation invocation; its usage is not captured by the app, so do not claim an exact combined bill. Bounded request/output sizes and retries keep this acceptance run within the approved allowance.

The browser showed the current frozen UTC window, Sponsor/inbound/current-direct-Closed-Won policy and OpenAI structured-output provenance. Its run succeeded in 7.087 seconds: 7 scanned contacts, 2 contacts, 2 threads, 5 messages, 5 exclusions and no failures. Exact expected identities, all normalization schemas, chronological positions, recent context, CC and attachment metadata pass. Canonical counts and 7 contact-company/3 contact-deal links are correct; re-enqueue deduplicates. Browser drawer and JSON verified. The actual downloaded CSV parses with all 8 headers and exactly 2 rows/5 messages; bytes match that run’s stored serialization. Live downloads were kept private outside Git.

**Validation/security.** Final `APP_MODE=demo pnpm check` exited 0: lint, strict types, 253 tests across 9 files (3.63 seconds), production build. Web restarted in live mode afterward. Observed worker logs contain only event/local-job-ID/count fields. No email/CRM data was sent to AI and no credential values were printed. Real-provider positive paths now pass; adversarial/failure/cleanup coverage remains mocked. Seven contacts are not a scale benchmark.

**Alternatives and debt.** No manual/rules interpretation substitution was used for final acceptance. Keep volatile provider attachment handles in raw provenance rather than claiming byte-identical cross-run files. Continuous sync, production quotas/observability, per-thread recovery and broad model evaluation remain deferred. Cosmetic mode-loading and queue-copy issues were recorded for approval before refinement.

**Interview points and owner action.** Explain constrained interpretation versus deterministic decisions; matching-window versus full context; durable jobs/idempotency; separate seed/read grants and receipt privacy; measured live proof versus mocked failure tests. Owner should review the baseline and rehearse; publication remains paused.

## Phase 6 — baseline report before refinement

Created `docs/baseline-report.md` with actual-versus-expected cases, timings, normalization/CSV observations, resolved failures, live/mock distinctions, UI findings, security/reliability limits and ranked improvements. Registered it in the explicit source manifest. Updated the primary live demo script and walkthrough; retained the labeled offline fallback. The technical baseline is complete, while owner acceptance/rehearsal/publication decisions remain separate. No refinement feature was started.

**Final checkpoint review.** The production web app restarted successfully after the final build; the saved natural-language run still renders Live data and Export ready. Explicit source-manifest review includes 119 files and finds no configured credential or approved-mailbox value. Both environment files remain mode 0600, ignored and untracked, as does the private receipt. Diff whitespace check passes. Only documentation/manifest changes occurred after the last passing build. A source-only handoff package is refreshed from this manifest; no live exports or runtime data belong in it. Local version-control checkpoint only; no push or new CI run.

## Post-baseline requirements audit and publication — 2026-09-16 Eastern

Owner explicitly authorized pushing all latest commits and reviewing/testing the original challenge before deciding improvements. Pushed the three local commits through `ed17f77`; GitHub main matched the local SHA. Fresh GitHub CI run 35176450319 passed 253 tests, lint/types/build, 11 browser/API tests in 23.7 seconds, and native PostgreSQL migrations/separate-worker/authentication/CSV checks using synthetic providers. This supersedes the prior historical CI limitation for that revision.

The Gmail connector profile matched the approved lab account. Label-specific counts confirmed 9 actual Gmail messages, 6 threads and zero Inbox matches; no unrelated email bodies were accessed. Code confirms the seed writer assigns the custom namespace label only and inserts historical messages without sending. Explained the distinction between seeding, live ingestion and offline demo providers.

Reviewed the saved original brief after fresh hosted retrieval failed. Gmail + free HubSpot is the stated starting slice, with Salesforce as the later Plum-stack integration. Added `requirements-verification.md` with requirement-to-evidence mapping, source-message search, architecture/data flow and the exact functionality to present. A bounded independent code review found a named-recipient-group normalization defect; regression coverage and a narrow fix are in progress. No new product architecture or provider is proposed.

The owner explicitly deferred the overview video/script and ElevenLabs narration until the whole application is ready. ElevenLabs is reportedly signed in in the browser; no audio generation, voice selection, charge or video recording occurred. Keep this deliverable pending until the final readiness review.

## Correctness checkpoint and fuller demo proposal — 2026-09-16 Eastern

**Implemented/data flow.** `src/domain/normalize.ts` now recursively flattens named recipient groups into individual identities before direction and eligibility matching. `tests/providers.test.ts` covers To, Cc, Bcc and the end-to-end Sponsor eligibility consequence. Keeping this at the normalization boundary fixes every downstream consumer; special-casing the executor would leave inconsistent identities.

**Actual validation.** Four regressions failed before the fix and passed afterward. Full `APP_MODE=demo pnpm check` exited 0: lint, strict types, 257 tests across nine files (3.72 seconds), production build. Production web and separate worker restarted successfully. These new group cases use synthetic fixtures; no new live export was invoked for this patch. Earlier live baseline acceptance remains recorded above.

**Security/limits/interview concepts.** No credential or correspondence logging added. This is MIME address handling, not an expanded mailbox permission. Explain grouped recipients versus individuals, normalized identity boundaries, direction versus sender-only matching, and regression tests that exercise a business consequence. Partial-provider-failure explanations and operational/UI polish remain separate improvements.

**Next phase goal and assumptions.** Owner requested more contacts and emails. Prepared an offline additive proposal: preserve original 7 contacts/9 messages, add 13 contacts/13 companies/5 deals/15 threads/50 messages; final 20 contacts/20 companies/8 deals/21 threads/59 messages. Expected representative result is 10 contacts, 12 contact-thread rows and 45 full-thread messages, with 10 excluded contacts. Two qualifying contacts have multiple distinct threads. All identities/content remain fictional, dates deliberately historical, no messages sent.

**Remaining work/owner action.** Scope confirmation is pending. The current writer intentionally rejects changed plan digests and assumes one thread per contact; implement an explicitly validated additive migration and mocked replay/failure tests before any live expansion. Do not reset the existing journal or widen the protected reader receipt before verification. Separate short-lived Gmail writer consent may need renewal. No expanded records have been written, and projected counts are not live evidence. Video and ElevenLabs remain on hold.

## Expanded seed implementation checkpoint — 2026-09-16 Eastern

Owner selected the 20-contact scope. `src/seed/plan.ts` now builds the original plan unchanged plus 13 fictional cases and explicit conversation keys. `writer.ts` verifies the original complete dataset before preparing a digest transition, then verifies preserved IDs, grouping, associations and final membership. `cli.ts` saves a private base journal before the transition and retains the existing reader receipt until complete verification. The offline preview supports `--expanded`. No runtime reader permissions or architecture changed.

Validation: expanded MIME → normalization → deterministic executor → CSV produces exactly 10 contacts, 12 rows and 45 messages, with 10 exclusions and no failures. Focused 41 seed tests pass; full lint/types/263 tests (3.68 seconds)/production build pass. Initial two new test assertions incorrectly treated read-only HubSpot token introspection POST as a record create; corrected assertions to distinguish that endpoint. No provider defect was hidden by this correction. Preservation, duplicate-free replay, unknown associations, incomplete base/wrong digest and interrupted creates are covered with mocked responses.

Separate writer authorization is expired; started the local consent helper and handed renewal to the owner. No expanded live writes yet. Alternatives rejected: deleting/reseeding would discard known provider IDs; changing a journal digest manually would bypass the intentional integrity guard. Remaining gates: safety review, consent, live preflight/apply/replay, protected-reader ingestion/CSV validation. Interview concepts: additive migrations, deterministic fixture truth sets, contact versus conversation identity, idempotency versus uncertain-write recovery, and verified receipt publication.

**Independent safety review.** Found a multi-thread verification gap: aggregate counts alone could accept crossed membership. Added an exact bijection between planned thread keys and returned Gmail thread IDs, plus a regression that crosses messages while retaining total/per-contact counts. Final full check passes 264 tests, lint/types and production build (3.77-second test run). Owner renewed seed OAuth and the helper confirmed private authorization saved. Expanded live writes remain pending.

**Owner-reported audience error.** Owner saw Build an audience fail while the web process was stopped for the production build. Browser showed Failed to fetch. Restored the server; a fresh actual OpenAI interpretation succeeded with correct Sponsor/inbound/window/Closed Won policy. Started its live run to verify completion before resuming expansion.

**Audience retest result.** The fresh browser OpenAI interpretation and live reader run succeeded: 2 contacts, 2 threads, 5 messages, 5 exclusions, zero failures, CSV link available. No missing integration was found. Temporary server unavailability explains the observed fetch error; announce future maintenance before stopping the app. This added one real interpretation invocation under the existing $1 allowance.

## Expanded live seeding completed — 2026-09-16 Eastern

Owner explicitly approved applying the reviewed additions after the dry-run, tested implementation and successful base preflight. Paused web/worker and applied plan digest `391c8c29010c5209ac631c4bfaa7b7976cf158cc85ed324a7c4bf0b5c6990ac2`. Final live verification passed: 20 contacts, 20 companies, 8 deals, 21 threads, 59 messages and exact associations/thread membership. A second expansion command replay passed, with every original ID preserved and no pending operation or duplicates. Gmail connector independently confirms 59 labeled messages in 21 threads. No messages were sent and no existing records were deleted. Private receipt was published only after verification.

Web and separate worker restarted. A fresh browser interpretation succeeded using real OpenAI, and the expanded live reader job is now running. GitHub commit `4bf2d3a` is pushed; CI run 35177756338 passed unit/lint/types/build, 11 browser/API checks (22.2 seconds) and native PostgreSQL migrations/separate-worker/auth/CSV verification with synthetic providers. Expanded live CSV acceptance is still pending at this checkpoint.

## Expanded live acceptance complete — 2026-09-16 Eastern

Fresh browser OpenAI interpretation → reviewed criteria → real reader job succeeded in 22.869 seconds. Actual counts match the independent truth set: 20 scanned, 10 included contacts, 12 threads, 45 messages, 10 exclusions, zero failures. Exact contact/thread pairs, message membership, normalized schemas and chronology pass. Canonical counts are 20/20/8 CRM, 12 matched threads/45 messages, and 20/8 associations. Alder's new four-message drawer preserves recent context, synthetic CC and attachment metadata. Actual browser CSV download privately parsed with eight exact columns, twelve rows and 45 messages; bytes equal stored serialization.

Updated `expanded-live-acceptance.md`, checklist, primary demo instructions and interview/readiness notes. This phase preserves the existing architecture and normal reader permissions. The additive approach retains IDs; exact grouping avoids count-only false positives. Real positive paths pass; adversarial failures/cleanup remain mocked, and 20 contacts do not establish production-scale performance. Credentials/receipts/CSV remain private; no email sent, no unrelated mail processed, no deletion.

Interview concepts: constrained interpretation versus deterministic execution; contact/thread cardinality; full context versus qualifying activity; migration/replay integrity; receipt privacy boundaries. No owner setup action remains. Rehearsal and recorded UI/error-message improvements remain; overview video/script/ElevenLabs explicitly deferred until final readiness.


## Presentation production checkpoint — 2026-09-17 Eastern

Goal: owner approved freezing implementation and preparing the narrated review demonstration, informed by the technical interview transcript. Focus on practical CRM/email integration, explainable selection, security boundaries and shipping tradeoffs. Treat transcript coaching blocks as unspoken suggestions, not interview evidence.

Implementation: ten-chapter authored script, live demo runbook, technical Q&A, and offline interactive walkthrough with eighteen composed visuals. Real application captures are mixed with explanatory diagrams and explicitly described as an edited walkthrough, not an uncut recording. Stock ElevenLabs narration generated from the authored non-sensitive script; no raw interview transcript uploaded, no voice cloning or new subscription. Media artifacts are in the task output folder, outside the source repository.

Validation: eighteen-frame contact sheet reviewed; browser initial render, Next navigation, chapter selection and terminal disabled Next passed. Screenshots mask account identifiers; raw captures, live CSV, tokens and transcript excluded from the 24-file rehearsal ZIP. Existing expanded live acceptance remains the application evidence; code was not changed or rebuilt during presentation production.

Decisions/assumptions: use actual verified app results and curated diagrams for a repeatable explanation; retain a separate click-by-click live runbook for interactive review. Do not claim production compliance or scale. Fictional Gmail/HubSpot records are live-service data; adversarial automated tests still use mocks.

Remaining: ElevenLabs export is generated but automated Download did not produce a local MP3. Manual download requested; do not regenerate. Final video assembly, audio synchronization, audiovisual QA and completed-video delivery remain unchecked. Optional production reliability work is deferred by owner direction.

Interview talking points: business value of joining systems of record; constrained AI versus deterministic eligibility; complete context versus matching activity; durable jobs and provider boundaries; honest production tradeoffs. Owner action: download the existing ElevenLabs MP3 to Downloads.


## Narrated video complete — 2026-09-17 Eastern

Owner supplied the existing ElevenLabs MP3. Assembled an 11:09 1080p H.264/AAC edited walkthrough with eighteen distinct visuals, ten embedded chapters and corrected selectable English captions. Local speech recognition aligned paragraph transitions; no audio was sent to another service and no narration was regenerated. MP4, source MP3, script, live runbook, Q&A, offline walkthrough, chapter list and validation report are packaged in the task output folder as plum-demo-complete.zip.

Validation: full audiovisual stream decode completed with zero errors; output duration 669.21 seconds; decoded conversation frame inspected for layout and privacy masks, supplementing the eighteen-frame composition review. In-app browser playback preview crashed, so successful browser playback is not claimed; deliver standard MP4 for local playback. No application code changed and no live provider processing occurred. Edited captures were chosen for repeatable narration; the separate runbook supports a real interactive demonstration. Production limitations and fictional/live data distinction remain explicit. Interview concepts: constrained AI, deterministic rules, conversation completeness, durable jobs, production tradeoffs. Owner action: rehearse in a local media player, then practice the live runbook.


## Setup guide and data-boundary wording — 2026-09-17

Goal: remove the private-GitHub dependency from the setup link and clarify data deletion and result cardinality. Added a static Next.js /setup-guide page with local setup, OAuth, HubSpot, interpretation, verification and removal instructions. SetupPanel links locally. ConnectionDetails and the delete confirmation explicitly distinguish app database deletion from untouched Gmail/HubSpot source data and downloaded files. ExportPreview explains one contact–thread pair per row. No deletion or provider mutation was executed; existing backend behavior and architecture remain unchanged.

Validation: lint, strict types, all 264 unit tests and production build passed. Four API end-to-end tests passed against isolated synthetic data. Seven browser tests could not launch because the local Playwright Chromium executable is missing (not application assertion failures). Direct browser inspection confirmed the new guide loads with its sections and back link without GitHub access. Web app rebuilt/restarted; administrator sign-in is required again. No new live ingestion was run. A local guide was selected over a private GitHub link for reviewer access; source docs retain detailed operator procedures. Remaining: owner sign-in and optional full browser suite rerun when Chromium is installed. Interview concepts: source versus derived data; credential retention versus deletion; foreign-key cascading; contact/thread cardinality.


## Repository documentation scope — 2026-09-18

Removed presenter scripts, timed walkthroughs and rehearsal Q&A from the current source tree at the owner’s request. Local practice copies are retained outside the repository. Architecture, functionality, setup, security and validation documentation remain. Updated README and source packaging manifest to remove obsolete references. This is a documentation-only change; application behavior is unchanged. Normal commit history is preserved.
